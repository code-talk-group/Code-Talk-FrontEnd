@model List<CodeTalk.Models.ApiResult>;


@{
    Layout = "_Layout";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Result Home</title>
</head>
<body>
    <section class="resultContainer">
        <div class="resultPageTitle">All Results: </div>
            <table class="table">
                <tbody>

                    @*<tr>
                            <td>
                                @Html.DisplayNameFor(model => model.Code)
                            </td>
                        </tr>*@
                    @foreach (var item in Model)
                    {
                        @*<tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Code)
                                </td>
                            </tr>*@
                        <tr>
                            <td class="allPrevious">
                                @Html.DisplayFor(modelItem => item.returnString)
                            </td>

                        <td class="allPrevious">
                            @*<a asp-action="Edit" asp-controller="Results" asp-route-id="@item.ID">Edit</a> |*@
                            <a asp-action="Delete" asp-controller="Results" asp-route-id="@item.ID">Delete</a>
                            @*<a  href="@Url.Action("Delete", "Results")">Delete</a>*@
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</section>
  </body>
</html>
